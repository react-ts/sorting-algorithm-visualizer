export const bubbleSort = (array: number []) => {
  let movements = []
  
  for(let i = 0; i < array.length; i++){
    for(let j = i + 1; j < array.length; j++){
      if(array[i] > array[j]){
        movements.push({
         
        })
      } else {

      }
    }
  }

  return movements;
}
